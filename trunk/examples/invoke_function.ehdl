const uint(1) en = 1;

int(32) c adder(int(32) a, int(32) b) { 
	
	c = a + b;  // perform check that c has the same type as return value
}


(int(32) c, int(32) d, int(8) g) main(int(32) e, int(32) f) {
	int(32) ar[2] = 1;
	(ar[0]) = adder(e,f);
	(ar[en]) = adder(e,f);
	
	(c) = adder(e,f); 
	(d) = adder(f,e(0:31)); 
	POS(1);
	POS(en);
	g = e(0:7);
	
}


